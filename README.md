# coderiver 🚀

Тестове завдання від CodeRiver, яке намагається вразити світ:

- ✨ **React** - бо 2025 рік, а PHP вже не модний
- 🎨 **Анімації** - щоб очі радувались, а не кровоточили
- 🎯 **Частинки** - бо нам подобається, коли все рухається
- 📱 **Адаптивність** - працює навіть на калькуляторі

## Що тут відбувається?

Створюємо крутий лендінг з:
- Hero секцією (обов'язково з великими буквами)
- Навігацією, що scramble при hover (інакше не серйозно)
- Частинками, що бігають за курсором (як коти за лазером)
- Анімаціями появи тексту (знизу вгору, як справжні профі)

## Запуск проекту

```bash
npm install
npm run dev
# і молимося, щоб все запрацювало з першого разу
```

**Статус:** В розробці 👨‍💻  
**Рівень стресу:** Середній 😅  
**Кава випито:** Ще недостатньо ☕
import reactDom from 'eslint-plugin-react-dom'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...
      // Enable lint rules for React
      reactX.configs['recommended-typescript'],
      // Enable lint rules for React DOM
      reactDom.configs.recommended,
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```
